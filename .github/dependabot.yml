version: 2
updates:
  # Cargo Dependabot cannot materialize the required ../fiducia-clients path
  # dependency. CI runs cargo audit against the lockfile instead.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
  - package-ecosystem: docker
    directory: "/"
    schedule:
      interval: weekly
