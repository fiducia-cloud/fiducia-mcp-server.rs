schema_version: ores.compose.v1
project: fiducia-mcp-server
allow_lazy_start: false

services:
  fiducia-mcp-server:
    name: fiducia-mcp-server
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/fiducia-mcp-server.rs"]
