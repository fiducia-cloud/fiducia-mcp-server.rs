schema_version: ores.compose.v1
project: fiducia-mcp-server.rs
allow_lazy_start: false

services:
  fiducia-mcp-server.rs:
    name: fiducia-mcp-server.rs
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/fiducia-mcp-server.rs"]
